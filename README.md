# ShopAssist AI

AI E-Commerce Customer Support frontend.

## Development

```sh
bun install
bun run dev
```

Set `VITE_BACKEND_URL` in `.env` to the backend URL. It defaults to
`http://localhost:8000`.
